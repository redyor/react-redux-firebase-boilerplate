import React from 'react';

const LoadingPage = ({ startLogout }) => (
  <div className="loader">
    <img src="/images/loader.gif" className="loader__image" />
  </div>
);

export default LoadingPage;
